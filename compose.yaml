# One stack on one host, per the plan's Deployment section.
#
# Two rules this file exists to enforce, both asserted in CI rather than trusted
# to review:
#
# 1. Only Caddy publishes a port. Photon, Valhalla, the renderer and the bot are
#    reachable only from inside this network; the bot in particular is the source
#    of authorization truth and must never be addressable from outside.
# 2. Every service declares both a memory and a CPU limit. The memory limits keep
#    the peak inside host RAM including the duplicate Valhalla containers that
#    exist during a swap; the CPU limit on the rebuild is what stops a six-hour
#    build from saturating every core and destroying the latency target.
#
# 3. Every `image:` names a registry. An unqualified name is a Docker Hub
#    reference, so `routemaker/api:dev` - which is what the four images in this
#    file used to be called - is `docker.io/routemaker/api:dev`: a namespace
#    this project does not own, which exists on Hub today with zero repos. A
#    `docker compose pull`, or an `up` on a host where the local build is
#    missing, would fetch whatever a stranger pushes there next and hand it
#    PGPASSWORD, KEY_ENCRYPTION_KEY and DJANGO_SECRET_KEY. The four are now
#    `ghcr.io/macrophage87/routemaker-<name>` - the namespace of this
#    repository's own owner - and `pull_policy: never` on the services that
#    `build:` takes the pull out of their path entirely. The
#    three third-party images are written `docker.io/...` for the same reason:
#    Hub is where they really come from, and saying so is what makes the
#    rendered reference unambiguous rather than a default.
#
# Secrets are scoped per service rather than handed to every container by a
# shared env_file, which would put the key-encryption key in Photon and Valhalla
# and contradict the claim that one component alone holds the bot token.

x-restart: &restart
  restart: unless-stopped

# Log rotation, on every service, because Docker's default has none.
#
# The default `json-file` driver writes an unbounded file under
# /var/lib/docker/containers on the *root* volume - the one PLAN:293 sizes
# small on purpose, and the one nothing in this stack binds. Nothing here is a
# quiet logger: gunicorn runs with `--access-logfile -`, so every request is a
# line; valhalla_service logs per request on three containers; the weekly
# rebuild runs for six hours and prints a stage at a time. There is no
# /etc/docker/daemon.json in this repository and none of it is a host this
# repository configures, so the rotation has to be declared per service, which
# is what this anchor is for.
#
# 10 MB x 5 files is a 50 MB ceiling per container and around 600 MB for the
# whole stack, which is the figure that has to fit beside the OS and the images
# rather than one chosen for how much history it keeps. A container's logs are
# for the last incident, not for the last month; what is kept deliberately is
# the ScheduledRun rows and the nightly dump, both of which are on the data
# volume.
x-logging: &logging
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "5"

# Everything `config/settings.py` reads from the environment that is not a
# secret and that every Django process needs the same value of. The four
# services that import Django settings - api, worker, migrate and rebuild - all
# merge this; the secrets stay written out per service below, because which
# container holds which secret is the thing the scoping rule exists to make
# readable at a glance.
#
# This block used to not exist and neither did most of its contents. Six
# variables were declared against the twenty-four settings.py reads, so
# ALLOWED_HOSTS was the module's own "localhost" default and every request
# arriving through Caddy was a DisallowedHost 400; the authorize URL carried an
# empty client_id and a localhost redirect; and the database host was the
# 127.0.0.1 default rather than the compose service. tests/test_compose.py
# derives the set of names from settings.py's source and fails until each one is
# either declared here or named in that test's allow-list.
#
# Defaults are repeated from settings.py only where an unset variable would
# otherwise reach it as the empty string (`int("")` raises at import) or where
# settings.py's own default is wrong inside a container - PGHOST, whose default
# 127.0.0.1 is this container rather than the database.
x-django-env: &django-env
  DJANGO_SETTINGS_MODULE: config.settings
  DJANGO_DEBUG: ${DJANGO_DEBUG:-}
  PGHOST: ${PGHOST:-postgis}
  PGPORT: ${PGPORT:-5432}
  PGDATABASE: ${PGDATABASE:-routemaker}
  PGUSER: ${PGUSER:-routemaker}
  DJANGO_CONN_MAX_AGE: ${DJANGO_CONN_MAX_AGE:-60}

services:
  caddy:
    <<: [*restart, *logging]
    image: docker.io/library/caddy:2.8-alpine
    ports: ["80:80", "443:443"]
    volumes:
      - ${DATA_ROOT}/caddy:/data
      - ${DATA_ROOT}/static:/srv/static:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    environment:
      # The one variable this service takes, and not a secret. The Caddyfile's
      # site address is `{$CADDY_SITE_ADDRESS}` - Caddy's own environment
      # substitution, read when the config loads - so one file serves a
      # deployment and a local stack: a hostname gets an automatic certificate,
      # `:80` serves plain HTTP. The default is the local one, because a
      # hostname guessed here would be a certificate request for someone else's
      # name.
      CADDY_SITE_ADDRESS: ${CADDY_SITE_ADDRESS:-:80}
    deploy:
      resources:
        limits: { memory: 256M, cpus: "0.5" }

  api:
    <<: [*restart, *logging]
    image: ghcr.io/macrophage87/routemaker-api:${TAG}
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    # Header rule 3: this image is built here and never pulled. Without it
    # compose's default policy for a service that also declares `build:` is
    # `missing` - a pull is attempted first on any host where the tag is not in
    # the local store, which is every host after a `docker system prune` and
    # every host that was given a `TAG` it has not built. As measured on
    # Compose v5.3.1, `up` under each policy:
    #
    #   policy                      tag in the store     tag absent
    #   missing / if_not_present    runs it              pulls, then builds
    #   build                       BUILDS, retags       builds
    #   never                       runs it              builds (no pull)
    #   always                      pulls first          pulls, then builds
    #
    # `build` is not a fallback: it rebuilds the working tree on every `up`,
    # so `TAG=<previous> up -d` would build today's code under yesterday's tag
    # and orphan the real previous image. `never` runs what the store holds
    # and pulls nothing; `docker compose build` is what makes a new image, and
    # `up -d --no-build` is the rollback form that refuses a tag the store has
    # lost instead of building the current tree under it (docs/DEPLOYMENT.md).
    pull_policy: never
    environment:
      <<: *django-env
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}
      KEY_ENCRYPTION_KEY: ${KEY_ENCRYPTION_KEY}
      PGPASSWORD: ${PGPASSWORD}
      # The host and origin settings. Both have defaults in settings.py that are
      # correct for a checkout and wrong for this stack: ALLOWED_HOSTS falls back
      # to "localhost", which behind Caddy makes every request a DisallowedHost
      # 400, and CSRF_TRUSTED_ORIGINS falls back to empty, which refuses every
      # POST that arrives over the proxy's HTTPS. Required, with no compose
      # default, because there is no value that is right for two deployments.
      DJANGO_ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS}
      DJANGO_CSRF_TRUSTED_ORIGINS: ${DJANGO_CSRF_TRUSTED_ORIGINS}
      DJANGO_ADMIN_PATH: ${DJANGO_ADMIN_PATH:-internal-8f3a/}
      # gunicorn's own worker count, and the default is this service's cpu
      # limit and not the host's cores. `docker/api-entrypoint.sh` derives one
      # when this is unset, and what it had to derive it from was `nproc`,
      # which reports the *host* - a cgroup cpu quota does not change it. On
      # PLAN:293's 8-vCPU host that was `8 * 2 + 1` = 17 sync gunicorn workers
      # inside the 2G/2-cpu limit below, each one a full Django process: an
      # arrangement that is memory-bound long before it is cpu-bound, and the
      # limit it hits is the one that gets a container OOM-killed. 5 is
      # `2 * 2 + 1` against the two cpus this service is actually given, which
      # is the same formula read off the right number. Raise it here if the
      # `cpus:` limit below is raised; the two are one decision.
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-5}
      # Discord sign-in, identify scope only. The client id and the redirect are
      # not secrets - they are in every authorize URL - but without them the URL
      # this service builds names an empty application and sends the browser back
      # to localhost, so the login path does not work at all. The secret is used
      # once per login to exchange a code and is read by this service alone.
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID}
      DISCORD_REDIRECT_URI: ${DISCORD_REDIRECT_URI}
      DISCORD_CLIENT_SECRET: ${DISCORD_CLIENT_SECRET}
      # The one bootstrap Discord id. It grants standing only while the
      # instance-admin list is empty, and the first admin request under it
      # writes the id into the list and disables this path for good. The admin
      # is the only surface that reads it, so it goes to this service alone.
      BOOTSTRAP_INSTANCE_ADMIN_DISCORD_ID: ${BOOTSTRAP_INSTANCE_ADMIN_DISCORD_ID:-}
      # The removal window. Read where a removal is scheduled, which is the
      # admin, which is this service.
      INSTANCE_ADMIN_REMOVAL_DELAY_SECONDS: ${INSTANCE_ADMIN_REMOVAL_DELAY_SECONDS:-3600}
      # Where the routing client finds each variant. The swap repoints the
      # ValhallaUpstream rows; these are the values a row is seeded from, so the
      # API and the rebuild that writes those rows must agree on them.
      VALHALLA_STANDARD_URL: ${VALHALLA_STANDARD_URL:-http://valhalla-standard:8002}
      VALHALLA_NO_TRAIL_URL: ${VALHALLA_NO_TRAIL_URL:-http://valhalla-no-trail:8002}
      VALHALLA_EBIKE_URL: ${VALHALLA_EBIKE_URL:-http://valhalla-ebike:8002}
      # The same /data this service's two mounts below are under, so
      # settings.TILES_DIR is /data/tiles and settings.STATIC_ROOT is
      # /data/static - both of them paths that exist in this container.
      # Without it settings.DATA_ROOT fell back to BASE_DIR/"data" and the
      # operations page's free-space block measured /app/data/tiles, which no
      # process ever creates: statvfs walked up to `/` and reported the
      # container's own writable layer as the room the next rebuild has.
      DATA_ROOT: /data
    # Two directories, and the api writes neither at request time.
    #
    # `tiles` is read-only and is there for one reader: the operations page's
    # free-space block, which is a statvfs on settings.TILES_DIR. The page is
    # rendered by *this* service, so the filesystem it reports on has to be one
    # this service can see; bound `:ro` because a statvfs is all it does with
    # it, and the promotion symlinks under it belong to `rebuild`.
    #
    # `static` is the collectstatic target, and it is writable because that is
    # the one deploy step that runs in this image (docs/DEPLOYMENT.md, "Static
    # assets"). Declaring it here is what lets that step be a plain
    # `docker compose run --rm api ./manage.py collectstatic --noinput` with no
    # `-v` built out of a shell variable - which is what the guide used to give,
    # and it needed `.env` sourced into the shell to fill it in.
    volumes:
      - ${DATA_ROOT}/tiles:/data/tiles:ro
      - ${DATA_ROOT}/static:/data/static
    # Liveness, against the view that does the cheapest thing that proves this
    # process can still answer: `/healthz` runs `SELECT 1` and returns 200 with
    # no body details.
    #
    # `python -c` and not `curl`: the runtime stage of docker/api.Dockerfile
    # installs no curl, and urllib is in the image by construction - it is the
    # interpreter gunicorn runs under.
    #
    # The `Host:` header is the whole reason this is not a one-liner.
    # settings.ALLOWED_HOSTS is DJANGO_ALLOWED_HOSTS, which on a real
    # deployment is the public name; a request to 127.0.0.1 with the matching
    # Host would be a DisallowedHost 400 and this check would be red for ever
    # on a healthy stack. So it sends the first name in that list, which is the
    # same string the deployment already had to get right for Caddy.
    #
    # Nothing is gated on this condition - `depends_on` here waits on migrate,
    # not on api - so a red healthcheck is a `docker compose ps` an operator
    # can read and not a stack that refuses to come up.
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import os,urllib.request as u; h=os.environ['DJANGO_ALLOWED_HOSTS'].split(',')[0].strip(); r=u.urlopen(u.Request('http://127.0.0.1:8000/healthz', headers={'Host': h}), timeout=5); raise SystemExit(0 if r.status == 200 else 1)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      # gunicorn forks WEB_CONCURRENCY full Django processes, and each one
      # imports the application before it accepts anything.
      start_period: 30s
    depends_on:
      postgis: { condition: service_started }
      migrate: { condition: service_completed_successfully }
    deploy:
      resources:
        limits: { memory: 2G, cpus: "2" }

  # The worker runs under manage.py so Django is set up before a task runs, and
  # its schema arrives through the migrate one-shot below like every other
  # table. The bare `procrastinate --app=... worker` this replaced died on a
  # missing schema function and, with the schema applied by hand, every task
  # raised AppRegistryNotReady. The queue list is explicit: the rebuild queue
  # belongs to the rebuild service, which is the container with the Valhalla
  # binaries and the data mounts.
  worker:
    <<: [*restart, *logging]
    image: ghcr.io/macrophage87/routemaker-api:${TAG}
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    pull_policy: never  # header rule 3, as on `api`.
    # Four slots, not the default one: every periodic task except the rebuild
    # queues here, and Procrastinate drops a periodic tick whose predecessor is
    # still running rather than delaying it. With one slot a nightly dump held
    # the queue and the 5-minute degraded sweep and 10-minute worker heartbeat
    # silently missed their ticks (docs/OPERATIONS.md, "The maintenance queue").
    command: ["./manage.py", "procrastinate", "worker", "--queues=maintenance", "--concurrency=4"]
    environment:
      <<: *django-env
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}
      KEY_ENCRYPTION_KEY: ${KEY_ENCRYPTION_KEY}
      PGPASSWORD: ${PGPASSWORD}
      # settings.BACKUP_DIR is DATA_ROOT/backups, and the only mount this
      # service has is the backups directory at /data/backups. Without this the
      # nightly dump resolved BACKUP_DIR against the module's default - a `data`
      # directory inside the image - and wrote every dump to container storage
      # that the next restart discards.
      DATA_ROOT: /data
    volumes:
      - ${DATA_ROOT}/backups:/data/backups
      # Read-only, and for one caller: `check_operations`. Its fourth check is
      # a statvfs on settings.TILES_DIR, and this is the service the cron entry
      # runs it in (docs/OPERATIONS.md, "Where the alerts are") - `worker` is
      # up whenever the stack is, including while `rebuild` is the container
      # being recreated. Without the bind the check measured this container's
      # writable layer and reported room the rebuild does not have.
      - ${DATA_ROOT}/tiles:/data/tiles:ro
    # The same sixty seconds, for the same reason, as the `rebuild` service
    # below: on SIGTERM Procrastinate cancels its side tasks and then calls
    # `unregister_worker`, which is a database round trip, and compose's
    # default is ten seconds between the SIGTERM and the SIGKILL.
    #
    # What it buys here is narrower than on the rebuild and still worth having.
    # Every task on this queue is bounded at thirty minutes
    # (config/procrastinate.py: BACKUP_TIMEOUT_S, SWEEP_TIMEOUT_S), so no grace
    # period anyone would accept on a `down` covers a running one either -
    # `shutdown_graceful_timeout` is unset, so the worker waits for the job
    # without a bound and reaches the SIGKILL. A SIGKILLed nightly_backup
    # leaves its row `doing` with no worker behind it, which the wedged-job
    # surface reports and `./manage.py unwedge_job <job_id>` clears. Sixty
    # seconds is what makes the *idle* case - which is the overwhelming
    # majority of `down`s and `up -d`s, since these tasks run for seconds a day
    # between them - a clean unregister rather than a race with the ten-second
    # default.
    stop_grace_period: 60s
    depends_on:
      postgis: { condition: service_started }
      migrate: { condition: service_completed_successfully }
    deploy:
      resources:
        limits: { memory: 2G, cpus: "2" }

  # Applies every migration, the worker's job tables included: procrastinate's
  # Django integration ships its schema as migrations, so this one step both
  # creates the schema on a fresh box and upgrades it on an existing one.
  migrate:
    <<: *logging
    image: ghcr.io/macrophage87/routemaker-api:${TAG}
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    pull_policy: never  # header rule 3, as on `api`.
    command: ["./manage.py", "migrate", "--noinput"]
    restart: "no"
    environment:
      <<: *django-env
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}
      # Every process that imports settings needs this: the ban tombstone's
      # HMAC key is derived from it and there is deliberately no default, so a
      # container without it fails at import rather than keying tombstones with
      # a value from the repository. That is why it reaches the two services
      # that only ever run migrations and the rebuild, neither of which
      # computes a tombstone.
      KEY_ENCRYPTION_KEY: ${KEY_ENCRYPTION_KEY}
      PGPASSWORD: ${PGPASSWORD}
    # `service_started` is the container running, which for PostgreSQL is a
    # process that has not finished recovery and is refusing connections. The
    # first `up` on a fresh volume is the case that matters: initdb, the PostGIS
    # extension and the first start take tens of seconds, and a migrate that
    # raced it exited with OperationalError - after which `api`, `worker` and
    # `rebuild`, all held on `service_completed_successfully`, never started at
    # all and the stack came up as nothing but Caddy and the routers.
    #
    # The health gate is on this service alone. The other three wait on migrate
    # having *completed*, which is a strictly later event than the database
    # being healthy, so gating them as well would only restate it.
    depends_on:
      postgis: { condition: service_healthy }
    deploy:
      resources:
        limits: { memory: 256M, cpus: "1" }

  postgis:
    <<: [*restart, *logging]
    image: docker.io/postgis/postgis:16-3.4
    # The same two names Django connects with, from the same variables and the
    # same defaults. They were literals here while every Django service read
    # ${PGUSER}/${PGDATABASE}, so a deployment that set either one in `.env`
    # created a database and a role this file never made, and the healthcheck
    # below - which has to probe the database the application uses - would have
    # inherited that split.
    environment:
      POSTGRES_DB: ${PGDATABASE:-routemaker}
      POSTGRES_USER: ${PGUSER:-routemaker}
      POSTGRES_PASSWORD: ${PGPASSWORD}
    # What `migrate` waits for, and the `-h 127.0.0.1` is the whole of it.
    #
    # `pg_isready` with no host probes the unix socket, which is exactly what
    # the image's init-phase server listens on: docker-entrypoint.sh starts a
    # temporary server with `listen_addresses=''` while initdb, the PostGIS
    # extension scripts and anything in /docker-entrypoint-initdb.d run. So the
    # socket answers PQPING_OK through the entire first-boot window this check
    # was added to cover - measured, not reasoned: socket probe rc=0 while a
    # TCP connect is still refused. The health gate opened early, migrate
    # started against a server that would not accept it, and the three services
    # held on `service_completed_successfully` never started at all.
    #
    # 127.0.0.1 over TCP is how every client outside this container connects,
    # migrate included, so it is what the gate has to measure. `-d` is not a
    # check that the database exists - PQPING_OK never touches it - it only
    # keeps the probe's parameters the same as the application's; what proves
    # the database and role are there is the first migration.
    #
    # The start period covers that first-boot work on an empty volume, and the
    # retries cover an ordinary restart.
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -h 127.0.0.1 -U ${PGUSER:-routemaker} -d ${PGDATABASE:-routemaker}",
        ]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 60s
    volumes: ["${DATA_ROOT}/postgres:/var/lib/postgresql/data"]
    deploy:
      resources:
        limits: { memory: 4G, cpus: "4" }

  # Pinned, like every other image in this file - PLAN:293's "all images
  # pinned" - because `latest` is whatever that repository's maintainer pushed
  # last and a `docker compose pull` on a bad week is a different geocoder with
  # no change in this repository. 2.4.0 is the newest release tag on Docker Hub
  # (rtuszik/photon-docker, pushed 2026-08-17); `latest`, `2` and `2.4` all
  # resolve to the same digest today, which is how the tag was chosen.
  #
  # Behind the same profile as the renderer and the bot, for a different
  # reason: this one has an image, and what that image does on a fresh host is
  # download the planet. Read out of the pinned tag's own source
  # (github.com/rtuszik/photon-docker at 2.4.0):
  #
  #   - `src/utils/config.py:36` puts the index at `/photon/data`.
  #     `/photon/photon_data` is the 1.x path, so the mount this service used to
  #     carry was inert - it bound a host directory the process never opened.
  #   - `src/utils/config.py:23` defaults INITIAL_DOWNLOAD to True, and REGION
  #     is unset, which means the whole-planet index: about 61 GB compressed,
  #     around 104 GB free needed to unpack it, and - with the mount pointing
  #     somewhere the image does not use - written to the container's writable
  #     layer on the *root* volume, which PLAN:293 sizes small on purpose.
  #     Short of that the entrypoint exits 75 and `restart: unless-stopped`
  #     crash-loops it from the first `up` on every new deployment.
  #
  # Nothing in phase 1 calls this service and PLAN:60's index import is
  # unbuilt (handoff.md section 7), so the profile is the honest place for it.
  # The two corrections below are what make enabling the profile safe rather
  # than a 61 GB surprise: the index lands under ${DATA_ROOT} on the data
  # volume, and nothing is fetched until someone populates it deliberately.
  # See docs/DEPLOYMENT.md, "Photon".
  #
  #     docker compose --profile unbuilt up -d   # once there is an index to serve
  photon:
    <<: [*restart, *logging]
    profiles: ["unbuilt"]
    image: docker.io/rtuszik/photon-docker:2.4.0
    environment:
      # No planet download on first boot. PLAN:60's filtered country dump is
      # what fills this index and it is unbuilt, so the only thing an initial
      # download could do here is fill the root volume. config.py:23.
      INITIAL_DOWNLOAD: "False"
    # config.py:36, not the 1.x `/photon/photon_data`.
    volumes: ["${DATA_ROOT}/photon:/photon/data"]
    deploy:
      resources:
        limits: { memory: 3G, cpus: "2" }

  # One process per tile variant. Worker counts are explicit because the layer 4
  # candidate set is issued concurrently under a per-modality latency bound, and
  # a single thor worker would serialise it.
  valhalla-standard:
    <<: [*restart, *logging]
    image: ghcr.io/valhalla/valhalla:3.5.1
    # The upstream image declares neither ENTRYPOINT nor CMD - it is a toolbox,
    # not a service image - so it inherits the base image's shell and exits
    # immediately. With `restart: unless-stopped` that is a container that
    # restarts forever while answering nothing, which is what an env var named
    # VALHALLA_CONFIG bought: the image reads no such variable.
    #
    # The second argument is the worker count per stage. valhalla_service takes
    # it from argv and otherwise uses hardware_concurrency, which on this host
    # would start one worker per core of the whole machine inside a two-core
    # limit. It is not a configuration key; the loki_workers, thor_workers and
    # odin_workers keys the configs used to carry are read by nothing.
    command: ["valhalla_service", "/conf/valhalla-standard.json", "2"]
    # The served build, at the path the config names. `current` is a symlink
    # the rebuild replaces at promotion; the container loads whatever it
    # points at when it starts, and is restarted after a swap to load the
    # next one. The same path resolves to the same file inside the rebuild
    # container, which binds ${DATA_ROOT}/tiles at /data/tiles - the same host
    # directory this line binds one variant's `current` out of.
    volumes:
      - ${DATA_ROOT}/tiles/standard/current:/data/tiles/standard/current:ro
      - ${DATA_ROOT}/elevation:/data/elevation:ro
      - ./valhalla:/conf:ro
      - ./lua:/conf/lua:ro
    deploy:
      resources:
        limits: { memory: 2G, cpus: "2" }

  valhalla-no-trail:
    <<: [*restart, *logging]
    image: ghcr.io/valhalla/valhalla:3.5.1
    # The upstream image declares neither ENTRYPOINT nor CMD - it is a toolbox,
    # not a service image - so it inherits the base image's shell and exits
    # immediately. With `restart: unless-stopped` that is a container that
    # restarts forever while answering nothing, which is what an env var named
    # VALHALLA_CONFIG bought: the image reads no such variable.
    #
    # The second argument is the worker count per stage. valhalla_service takes
    # it from argv and otherwise uses hardware_concurrency, which on this host
    # would start one worker per core of the whole machine inside a two-core
    # limit. It is not a configuration key; the loki_workers, thor_workers and
    # odin_workers keys the configs used to carry are read by nothing.
    command: ["valhalla_service", "/conf/valhalla-no-trail.json", "2"]
    # The served build, at the path the config names. `current` is a symlink
    # the rebuild replaces at promotion; the container loads whatever it
    # points at when it starts, and is restarted after a swap to load the
    # next one. The same path resolves to the same file inside the rebuild
    # container, which binds ${DATA_ROOT}/tiles at /data/tiles - the same host
    # directory this line binds one variant's `current` out of.
    volumes:
      - ${DATA_ROOT}/tiles/no-trail/current:/data/tiles/no-trail/current:ro
      - ${DATA_ROOT}/elevation:/data/elevation:ro
      - ./valhalla:/conf:ro
      - ./lua:/conf/lua:ro
    deploy:
      resources:
        limits: { memory: 2G, cpus: "2" }

  valhalla-ebike:
    <<: [*restart, *logging]
    image: ghcr.io/valhalla/valhalla:3.5.1
    # The upstream image declares neither ENTRYPOINT nor CMD - it is a toolbox,
    # not a service image - so it inherits the base image's shell and exits
    # immediately. With `restart: unless-stopped` that is a container that
    # restarts forever while answering nothing, which is what an env var named
    # VALHALLA_CONFIG bought: the image reads no such variable.
    #
    # The second argument is the worker count per stage. valhalla_service takes
    # it from argv and otherwise uses hardware_concurrency, which on this host
    # would start one worker per core of the whole machine inside a two-core
    # limit. It is not a configuration key; the loki_workers, thor_workers and
    # odin_workers keys the configs used to carry are read by nothing.
    command: ["valhalla_service", "/conf/valhalla-ebike.json", "2"]
    # The served build, at the path the config names. `current` is a symlink
    # the rebuild replaces at promotion; the container loads whatever it
    # points at when it starts, and is restarted after a swap to load the
    # next one. The same path resolves to the same file inside the rebuild
    # container, which binds ${DATA_ROOT}/tiles at /data/tiles - the same host
    # directory this line binds one variant's `current` out of.
    volumes:
      - ${DATA_ROOT}/tiles/ebike/current:/data/tiles/ebike/current:ro
      - ${DATA_ROOT}/elevation:/data/elevation:ro
      - ./valhalla:/conf:ro
      - ./lua:/conf/lua:ro
    deploy:
      resources:
        limits: { memory: 2G, cpus: "2" }

  # No source, no `build:`, and therefore no image anywhere: `docker compose up`
  # stopped at a pull of routemaker/renderer:${TAG} from a registry that has
  # never held it. Parked behind a profile so the default `up` skips it and the
  # stack that *can* start does. handoff.md section 7 carries both rows; the
  # service stays here, with its limits, so the sizing arithmetic and the
  # no-published-ports rule keep counting it.
  #
  #     docker compose --profile unbuilt up -d   # once there is something to run
  renderer:
    <<: [*restart, *logging]
    profiles: ["unbuilt"]
    image: ghcr.io/macrophage87/routemaker-renderer:${TAG}
    deploy:
      resources:
        limits: { memory: 1G, cpus: "1" }

  # The only component that holds the bot token, and the source of authorization
  # truth. Exposes its membership interface on the internal network behind a
  # shared secret, read-only.
  # Behind the same profile as the renderer and for the same reason: there is no
  # bot source in this repository, so there is no routemaker/bot image to pull.
  # handoff.md section 7's first row. What follows from its absence is written
  # down rather than discovered - the membership cache is only as fresh as the
  # last hand-run sweep, and nothing answers the API's membership queries.
  bot:
    <<: [*restart, *logging]
    profiles: ["unbuilt"]
    image: ghcr.io/macrophage87/routemaker-bot:${TAG}
    environment:
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN}
      BOT_INTERNAL_SECRET: ${BOT_INTERNAL_SECRET}
      PGPASSWORD: ${PGPASSWORD}
    depends_on: [postgis]
    deploy:
      resources:
        limits: { memory: 512M, cpus: "1" }

  # The weekly rebuild's executor: a worker on the rebuild queue alone, in the
  # image that carries the Valhalla and GDAL binaries. The scheduling is
  # Procrastinate's (the periodic task is registered in every worker and the
  # deferral is deduplicated in the database), the execution is here, because
  # this is the container with the binaries, the mounts and the limits. Takes
  # half the cores.
  #
  # DATA_ROOT inside it is /data, and the five directories it writes are bound
  # one by one under that path - see the note on `volumes:` below for why it is
  # five binds and not the whole of ${DATA_ROOT}. Every path the generated
  # Valhalla configs name - the tile directories under /data/tiles, the
  # elevation cache at /data/elevation - therefore resolves to the same host
  # file the serving containers mount. The earlier version mounted /tiles and
  # /extracts, neither of which any configured path named, so tiles landed on
  # ephemeral container storage and vanished with it.
  #
  # Phase 1 validates a build by reading it back with valhalla_service in
  # one-shot mode inside this container; it starts no duplicate serving
  # containers, so this service is the only extra memory resident at swap
  # time. When blue/green validation containers arrive, this worker has to be
  # out of the way for the plan's swap-time arithmetic to hold - see
  # scripts/check_compose_limits.py, which asserts both figures.
  rebuild:
    <<: [*restart, *logging]
    image: ghcr.io/macrophage87/routemaker-pipeline:${TAG}
    build:
      context: .
      dockerfile: docker/pipeline.Dockerfile
    pull_policy: never  # header rule 3, as on `api`.
    command: ["./manage.py", "procrastinate", "worker", "--queues=rebuild", "--concurrency=1"]
    # Compose's default is 10 seconds between the SIGTERM and the SIGKILL, and
    # what this worker owes the database on the way out is a real shutdown:
    # Procrastinate cancels its side tasks (the periodic deferrer, the
    # heartbeat) and then calls `unregister_worker`, which is a database round
    # trip on the same PostgreSQL the rebuild has had four cores' worth of load
    # on. A minute is what makes that the ordinary outcome of `down`, `up -d`
    # and `restart` rather than a race, and it is short enough that a `down` is
    # still a `down`.
    #
    # What it does *not* do is save a rebuild that is mid-build, and the reason
    # is in procrastinate 3.9.0's worker.py: on SIGTERM the worker stops
    # fetching and then `_shutdown` awaits the running jobs with
    # `shutdown_graceful_timeout`, which is unset here and therefore None - it
    # waits for the job, with no bound. `weekly_rebuild` is a synchronous task
    # that polls nothing, so even the post-timeout abort path would only log
    # ("Job is responsible for aborting by checking context.should_abort") and
    # keep waiting. A six-hour build reaches the SIGKILL whatever this value
    # is, and a SIGKILL leaves the job row `doing` with no worker behind it:
    # `./manage.py unwedge_job <job_id>` is what moves it back to `todo`. Only
    # a run that *finishes* inside the grace ends marked by itself.
    stop_grace_period: 60s
    environment:
      <<: *django-env
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}
      KEY_ENCRYPTION_KEY: ${KEY_ENCRYPTION_KEY}
      PGPASSWORD: ${PGPASSWORD}
      DATA_ROOT: /data
      # The disk gate: a rebuild refuses to start unless a second full tile set
      # fits beside the current one. This is the only service that reads it.
      REBUILD_MIN_FREE_BYTES: ${REBUILD_MIN_FREE_BYTES:-21474836480}
      # The source extract the rebuild produces for itself (docs/OPERATIONS.md,
      # "The source extract"). All four are optional overrides of the defaults
      # in settings: a mirror, the freshness limit, a forced refresh, and a
      # coverage polygon in place of the bounding box.
      SOURCE_EXTRACT_URLS: ${SOURCE_EXTRACT_URLS:-}
      SOURCE_EXTRACT_MAX_AGE_DAYS: ${SOURCE_EXTRACT_MAX_AGE_DAYS:-6}
      SOURCE_EXTRACT_FORCE_REFRESH: ${SOURCE_EXTRACT_FORCE_REFRESH:-}
      COVERAGE_POLYGON: ${COVERAGE_POLYGON:-}
      # The rebuild writes the ValhallaUpstream rows at the swap, seeding any
      # row that does not exist yet from these.
      VALHALLA_STANDARD_URL: ${VALHALLA_STANDARD_URL:-http://valhalla-standard:8002}
      VALHALLA_NO_TRAIL_URL: ${VALHALLA_NO_TRAIL_URL:-http://valhalla-no-trail:8002}
      VALHALLA_EBIKE_URL: ${VALHALLA_EBIKE_URL:-http://valhalla-ebike:8002}
    # The five directories this service writes, each bound on its own, rather
    # than `${DATA_ROOT}:/data` - which is the same volume and also carries
    # `caddy/` (the ACME account key and the deployment's TLS private key),
    # `postgres/` (PGDATA) and `backups/` (a dump of the whole database). A
    # rebuild has no business reading any of the three, and handing them to it
    # is the same rule this file's header states about secrets: scoped per
    # service, so that what one container can reach is readable at a glance.
    #
    # `DATA_ROOT: /data` above is unchanged and so is every path derived from
    # it - settings.TILES_DIR is /data/tiles, REBUILD_SOURCE_PBF is
    # /data/extracts/source.osm.pbf, REBUILD_REFERENCE_DIR is /data/reference,
    # REBUILD_WORK_DIR is /data/rebuild - because each now resolves into its
    # own mount at the same path. The five sources are all under ${DATA_ROOT},
    # so they remain one filesystem and the rebuild's disk gate, which measures
    # TILES_DIR and is asked whether the extract will fit, still speaks for
    # both (tests/test_source.py holds that).
    volumes:
      - ${DATA_ROOT}/tiles:/data/tiles
      - ${DATA_ROOT}/elevation:/data/elevation
      - ${DATA_ROOT}/extracts:/data/extracts
      - ${DATA_ROOT}/reference:/data/reference
      - ${DATA_ROOT}/rebuild:/data/rebuild
      - ./valhalla:/conf:ro
      - ./lua:/conf/lua:ro
    depends_on:
      postgis: { condition: service_started }
      migrate: { condition: service_completed_successfully }
    deploy:
      resources:
        limits: { memory: 8G, cpus: "4" }
