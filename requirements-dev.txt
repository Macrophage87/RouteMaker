# Pinned loosely here and exactly in the image build; see Operations in the plan.
django>=5.0
django-ninja
psycopg2-binary
procrastinate
defusedxml
pytest
pytest-django
ruff
pyyaml
osmium
shapely

# Used by the worker-schedule tests to derive each alert window from its cron
# expression rather than restate it. A transitive dependency of procrastinate,
# declared here because the suite imports it directly.
croniter
